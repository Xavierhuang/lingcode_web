# Secrets — never commit. Copy the *.example files and fill in real values on
# the host (or inject at deploy). Only the .example templates are tracked.
pgbouncer/userlist.txt
pgbackrest/pgbackrest.conf
# Observability: rendered-from-.env configs that carry secrets/targets.
observability/prometheus.yml
observability/alertmanager.yml
observability/metrics_token
*.env
.env
