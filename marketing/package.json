{
  "name": "lingcode-marketing",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Vertical-video pipeline for /try.html demo capture.",
  "scripts": {
    "record": "node record-demo.mjs",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.1"
  }
}
