node_modules/

# SQLite database + its runtime journals and backups — never commit
# (the .bak-* dumps contain real production/user data)
data.db
data.db-shm
data.db-wal
data.db.bak*

# Env files: .env.example IS tracked; everything else (live config, backups) is not
.env
.env.bak
.env.*.bak

*.log
